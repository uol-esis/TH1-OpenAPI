type: object
required:
  - keywords
  - ignoreCase
  - matchType
properties:
  converterType:
    $ref: './converterType.yaml'
  keywords:
    type: array
    items:
      type: string
    description: Liste von Keywords zur Spaltenauswahl
    example: [ "email", "userEmail" ]
  ignoreCase:
    type: boolean
    description: Ob die Groß-/Kleinschreibung ignoriert wird
    example: true
  matchType:
    $ref: './enums/matchType.yaml'
